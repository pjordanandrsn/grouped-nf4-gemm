---
name: Hardware-wanted confirmatory run
about: Report an on-silicon measurement on a device we can't test in-fleet
title: "[hw] <device> confirmatory"
labels: ["hardware-wanted"]
---

**Device**: (e.g. AMD MI300X / Intel Arc B580 / RTX 5090)
**Backend / toolchain**: (ROCm x.y / oneAPI / CUDA x.y, Triton or SYCL)

**What I ran**: (hw_contract.py? census sweep? which shapes?)

**Receipt** (paste JSON or link):

```
```

**Notes / caveats**: (thermals, shared box, driver quirks — anything that
would change how the number should be read)
